#!/usr/bin/env bash
# Khal.js is deliberately Qt-free so the parsing and grouping rules can be
# checked here rather than by opening the panel and looking.
set -euo pipefail
cd "$(dirname "$0")/.."
node test/khal-test.js "$PWD/Khal.js"
